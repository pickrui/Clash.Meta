These small synthetic MaxMind DB fixtures map every IPv4 address to the value
in their filename. The country files use the `sing-geoip` format; the ASN files
use `GeoLite2-ASN`, with organization names `Test Network A` and `Test Network B`.
They contain one 24-bit search-tree node whose two branches point to the same
record. They contain no real geolocation data.
